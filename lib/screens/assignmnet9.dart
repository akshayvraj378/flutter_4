import 'package:flutter/material.dart';

class AS9 extends StatelessWidget {
  const AS9({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(decoration: const BoxDecoration(shape: BoxShape.circle)),
    );
  }
}
